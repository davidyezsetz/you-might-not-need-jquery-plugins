/* global List */
(function(List) {
  /* jshint unused: false */
  'use strict';

  var options = {
    valueNames: [ 'name', 'categories' ]
  };

  var userList = new List('js_plugins', options);
})(List);
