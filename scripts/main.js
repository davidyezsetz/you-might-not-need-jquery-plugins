/* global List */
(function(List) {
  /* jshint unused: false */
  'use strict';

  var options = {
    valueNames: [ 'js_plugin-name', 'js_plugin-categories' ]
  };

  var userList = new List('js_plugins', options);
})(List);
